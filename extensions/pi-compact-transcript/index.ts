// PiAstra-managed fork of pi-compact-transcript (MIT, © Alan Hagedorn),
// vendored from pnpm store package pi-compact-transcript@0.10.1 and modified.
// This entry file exists so Pi's directory auto-discovery (*/index.ts) finds
// the vendored copy; the extension itself lives in ./extensions/compact-transcript.ts.
export { default } from "./extensions/compact-transcript.ts";
